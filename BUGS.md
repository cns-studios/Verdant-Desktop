* Verdant starting in foreground on boot (Windows)
* Mail fetching (BG) is very slow and unreliable (Gmail, IMAP doesnt receive any notifications)
* If manually refreshing (CTRL + R) the "Fetching mails" popup shows multiple times
* Launching Verdant via App launcher spawns a new instance instead of opening the existing one